NLog can find the configuration file by following settings:
1.Create a config file named "NLog.config" or use app.config or web.config
2.Build Action = "Content" and Copy to Output Directory = "Copy always"